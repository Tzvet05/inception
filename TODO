0) Makefile
Add echos
Add start & stop ?
rm data/ folder

1) MariaDB

To test :
If root privileges given to mariadb client in config file are not sufficient for privileges, add "chown -R mysql:mysql /run/mysqld" to give user & group privileges to the directory

2) WordPress

Todo :
Dockerfile
Config file
Entrypoint script

Create 2 users (1 admin)
Add environment section to docker-compose

3) NginX

Todo :
SSL certificate



MISC :

To check :
Is this command needed ?
rm -rf /var/lib/apt/lists/*

Use a single shared alpine package
rm gitignore ?
rm todo
